# Deferred Work Ledger

- source_spec: `_bmad-output/implementation-artifacts/spec-mcp-tool-wrappers.md`
  summary: `mcp_server.tools.track_application` has no audit trail (who/when/what) for its mutating calls (mark applied/discarded).
  evidence: An MCP client can trigger a permanent tracker-state mutation with no logging anywhere of the invocation, making it impossible to reconstruct after the fact what an autonomous agent changed and when — flagged in the 2026-07-11 adversarial review of `spec-mcp-tool-wrappers.md`.
  status: done 2026-07-12
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-mcp-track-application-audit-trail.md` — new `mcp_audit_log` table written by `track_application` for every attempt reaching a DB connection (success, unknown-job-id rejection, mutation-raises rejection); invalid-action calls stay a zero-I/O fast-fail and are not logged (resolved intent_gap). 153/153 tests passing.

- source_spec: none
  summary: Add a `requirements.txt` (with `-e .`) so this app can be deployed to Streamlit Community Cloud, which doesn't use `uv`.
  evidence: Split from a combined "prep for Streamlit Cloud deployment" intent on 2026-07-11 — independently shippable, no coupling to the tailoring-storage change or the Admin page, deferred in favor of tackling the higher-risk tailoring-storage change first.
  status: done 2026-07-12
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-requirements-txt-streamlit-cloud.md` — added a one-line `requirements.txt` (`-e .`); verified with a clean `pip install -r requirements.txt` in an isolated venv (no `uv`) that it correctly builds and installs the app plus all dependencies. Documented in README. Review surfaced two new deploy-readiness gaps (secrets bridging, dependency-pinning drift), logged as separate deferred entries above — the requirements.txt file itself is complete and working.

- source_spec: none
  summary: Add an "Admin" Streamlit page with (a) CV upload -> `resume.extract`/`resume.db` profile registration and (b) a sponsor-register-ingest button calling `register.ingest.ingest(...)` — needed because Streamlit Cloud has no shell access and `data/`/`cv/`/`.env` are gitignored, so a fresh cloud deploy starts empty.
  evidence: Split from the same "prep for Streamlit Cloud deployment" intent on 2026-07-11 — independently shippable, deferred in favor of tackling the higher-risk tailoring-storage change first.
  note: Split further on 2026-07-12 into two independent goals — see the new "CV upload -> resume registration" entry below for the (a) half. This entry's (b) half (sponsor-register-ingest button) is being implemented now.
  status: done 2026-07-14
  resolution: Manual sweep triage confirmed implemented — `views/admin.py:45-57` wires the "Refresh sponsor register now" button to `register.ingest.ingest(...)`, committed in 7d772b8 ("Add Admin page with sponsor-register-ingest button").

- source_spec: none
  summary: Add a CV-upload control to the Admin Streamlit page that extracts resume text and registers a profile via `resume.extract.extract_profile`/`resume.db.insert_profile` — the (a) half of the original combined "Admin page" deferred item.
  evidence: Split from the combined Admin-page intent on 2026-07-12 — independently shippable from the sponsor-register-ingest button (unrelated domain: `resume.db`/profile.db vs. sponsors.db), and non-trivial on its own: no docx-to-text extraction helper exists yet (the CLI's `_read_input` only reads plain `.txt`; a real `.docx` CV upload needs new logic, likely via the already-installed `python-docx`, to get raw text out first). Deferred in favor of shipping the simpler, already-complete-pipeline register-ingest button first.
  status: done 2026-07-14
  resolution: Manual sweep triage confirmed implemented — `views/admin.py:73-100` adds the CV `st.file_uploader` + "Extract & Register Profile" button, calling `resume.extract.extract_text_from_docx`/`extract_profile` and `resume.db.insert_profile`, committed in f95ea34 ("Add CV-upload resume profile registration to the Admin page").

- source_spec: `_bmad-output/implementation-artifacts/spec-tailored-content-file-only-storage.md`
  summary: Stop persisting outreach-message text (`outreach_messages.message`) in `jobs.db` — write drafted outreach messages to `cv/generated_cv/<company>/{job_id}_outreach_{channel}.txt` instead, keeping only metadata (channel, contact_name, char_count) in the DB.
  evidence: Split out on 2026-07-11 because the spec was ~2900 tokens (well over the 1600 target). Outreach has no caching bug to fix (it was never cache-checked, always freshly generated), unlike resume/cover-letter — bundled only by the shared "stop storing full text in DB" theme, not by any shared mechanism or risk. Narrowing the spec to the resume/cover-letter fix + legacy-data migration keeps the two genuinely coupled concerns together.
  status: done 2026-07-14
  resolution: Manual sweep triage confirmed implemented — `src/jobs/outreach_db.py`'s `outreach_messages` schema (line 26) has no `message` column, only `char_count`; messages now write to disk via `_outreach_message_path` in `src/jobs/cli.py`. Committed in 6e010bf ("Stop persisting outreach-message text in jobs.db"). Note: this entry's two derived gaps (regenerate-button no-op, write-failure-no-rollback) remain separately open below.

- source_spec: `_bmad-output/implementation-artifacts/spec-tailored-content-file-only-storage.md`
  summary: `views/intake.py`'s and `views/jobs_list.py`'s "Regenerate tailored resume & cover letter" button always calls `generate_tailored_docx_for_job` with the default `force=False`, so clicking it when the docx cache is already valid is silently a no-op cache hit, not an actual regeneration — the button's label overpromises.
  evidence: Flagged in round-2 adversarial review (2026-07-12) of `spec-tailored-content-file-only-storage.md`. Pre-existing gap, not introduced by this spec's refactor — the UI never had a way to force regeneration before or after this change; the refactor only touched the caching mechanism, not this UI wiring. A real fix would add an explicit "force regenerate" control, which is a UI-design decision out of scope for this refactor.
  status: done 2026-07-15
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-force-regenerate-control.md` — no new UI control needed after all; both views already computed `already_generated` to pick the button's own label, so reusing that boolean as a new `force` parameter on `generate_tailored_docx_for_job` (`src/jobs/ui_actions.py`) makes the button's behavior match its label with zero new UI surface. Review caught that the initial tests only exercised `generate_tailored_docx_for_job` directly, not the real view call sites — added 2 `AppTest`-based tests (`tests/test_ui_actions.py`) that seed a pre-existing docx cache, click the real "Regenerate" button, and assert `force=True` reaches the mock; also strengthened the existing error-display tests to assert `force=False` on the "Generate" (no-cache) path. 195/195 tests passing (4 new).

- source_spec: `_bmad-output/implementation-artifacts/spec-tailored-content-file-only-storage.md`
  summary: `_cmd_tailor` (and the shared tailoring-generation path) assumes `TailoredApplication.evidence_notes`/`.portfolio_gaps` are always lists with no defensive check; every test mocks the LLM call so this assumption is never exercised against a malformed real response.
  evidence: Flagged in round-2 adversarial review (2026-07-12). Pre-existing assumption that predates this refactor (relies on the `TailoredApplication` pydantic model's contract) — not caused by this diff, just surfaced incidentally while reviewing it.
  status: done 2026-07-15
  resolution: Manual sweep triage (2026-07-15) found the premise didn't hold — `TailoredApplication` (`src/jobs/tailor.py:35-39`) is a pydantic `BaseModel` with `evidence_notes: list[str]`/`portfolio_gaps: list[str]`, parsed via `TailoredApplication.model_validate_json(...)` (`tailor.py:92`). A malformed real LLM response fails loudly with a pydantic `ValidationError` at the parse boundary; it can't silently reach `_cmd_tailor` as a bad assumption. No code change needed.

- source_spec: `_bmad-output/implementation-artifacts/spec-tailored-content-file-only-storage.md`
  summary: None of the `.txt`/`.docx` write paths in `jobs/cli.py` (tailoring output, legacy-migration backup) use a temp-file-then-rename pattern, so a crash mid-write can leave a partial file that still passes the "does it exist" cache/already-generated checks used throughout this codebase.
  evidence: Flagged in round-2 adversarial + edge-case review (2026-07-12). Pre-existing pattern across the whole codebase (no file write anywhere uses atomic replace), not specific to this diff — a real hardening item but broader than this spec's scope.
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-atomic-file-writes.md` (CAP-1, CAP-2) — new shared `_atomic_write_text()` helper in `jobs/cli.py` (temp-file-then-`os.replace`, mirrors `_bmad/scripts/memlog.py`'s own `write_atomic()`), routed through `_write_tailoring_files` (lines 242-243), `_migrate_legacy_text` (535, 539), and `_migrate_legacy_outreach_text` (663, composing with its existing per-row warn-and-continue `try/except OSError`). 173/173 tests passing (3 new: happy-path + simulated-`os.replace`-failure coverage for the helper itself).

- source_spec: none
  summary: `.gitignore` excludes `data/*.db` and `cv/` (personal resume/CV content) but not `data/tailored/` — the plain-text `tailor` CLI command and `migrate-legacy-tailoring`'s DB-text-backup step both write real tailored-resume/cover-letter text there, which is currently untracked-but-committable in this public repo.
  evidence: Discovered 2026-07-12 while committing the tailored-content-file-only-storage spec's implementation — `data/tailored/1_resume.txt`/`1_cover_letter.txt` (real personal content, written by this session's `migrate-legacy-tailoring` run) showed up as untracked (`??`) rather than ignored. Deliberately left out of that commit; `.gitignore` should add `data/tailored/` (or a broader `data/` pattern) before any future `git add` in this area.
  status: done 2026-07-12
  resolution: Added `data/tailored/` to `.gitignore`; confirmed via `git status` that the directory no longer shows as untracked.

- source_spec: `_bmad-output/implementation-artifacts/spec-requirements-txt-streamlit-cloud.md`
  summary: On a real Streamlit Community Cloud deploy, `app.py`'s `load_dotenv()` will silently no-op (no `.env` file ships to Cloud) and `GEMINI_API_KEY` will be `None` — Cloud delivers secrets via `st.secrets`, not `os.environ`, and nothing in the codebase bridges the two.
  evidence: Flagged in the adversarial review (2026-07-12) of the `requirements.txt` one-shot change. `requirements.txt` only fixes the *build* step; every Gemini-calling code path (`jobs/extract.py`, `jobs/tailor.py`, `jobs/outreach.py`) would still fail at the first LLM call on a fresh Cloud deploy without this fix.
  status: done 2026-07-12
  resolution: `app.py` now bridges `st.secrets["GEMINI_API_KEY"]` into `os.environ` on startup when not already set (env/`.env` always wins if present); swallows the case where no secrets source exists at all (expected for local dev), and prints a stderr diagnostic if the key is still missing after all sources are checked. Verified manually against a real `st.secrets` object (temporary `.streamlit/secrets.toml`, now gitignored) in both the present and absent cases. 153/153 tests still passing (no existing test covers `app.py` itself — see the new deferred entry below on why that's out of scope for this fix).

- source_spec: `_bmad-output/implementation-artifacts/spec-gemini-api-key-secrets-bridge.md`
  summary: The `GEMINI_API_KEY` secrets-bridging logic in `app.py` has no automated test — `app.py` executes `st.set_page_config`/`st.navigation`/`pg.run()` as a side effect of module import, so a plain pytest import would run the whole page router, not just the bridging logic.
  evidence: Flagged in adversarial review (2026-07-12) of the secrets-bridge fix. Fixing it means deciding where the bridging logic should live to be independently testable (e.g. extracting it into a small importable helper module) — a real design decision, not a trivial patch, and `app.py`/`views/*.py` have no existing test coverage or testing pattern to follow in this codebase today.
  status: done 2026-07-15
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-streamlit-view-testability.md` (CAP-1) — no extraction needed after all; `streamlit.testing.v1.AppTest` (already available, `streamlit==1.58.0`) runs `app.py` as a real script in a sandboxed runtime. New `tests/test_app.py` covers all 3 branches: secrets-present/env-unset, env-already-set (secrets don't override), and neither-present (env stays unset, stderr warning fires). New `tests/conftest.py`'s `streamlit_data_env` fixture (chdir + seed 3 DBs) makes running `app.py`/`views/*.py` safely testable. 191/191 tests passing (6 new).

- source_spec: `_bmad-output/implementation-artifacts/spec-requirements-txt-streamlit-cloud.md`
  summary: `requirements.txt` uses a floating `-e .` install (pip resolves fresh against `pyproject.toml`'s loose version floors) rather than a frozen list generated from `uv.lock`, so Streamlit Cloud can silently resolve a different dependency graph than what's tested locally — and nothing detects that drift since this repo intentionally has no CI.
  evidence: Flagged in the adversarial review (2026-07-12) of the `requirements.txt` one-shot change. A deliberate trade-off for now (matches the deferred-work item's original "-e ." instruction and this project's no-CI-by-design convention), but worth revisiting if a Cloud deploy ever breaks from an unexpected dependency bump.

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-sponsor-register-ingest.md`
  summary: `register.cli.DEFAULT_SOURCE` is one specific dated gov.uk CSV snapshot, not an auto-updating "latest register" endpoint — every click of the new Admin page's "Refresh sponsor register now" button re-fetches that exact same frozen file forever, and there's no way to point it at a newer release without editing the constant and redeploying (a custom-source input was explicitly ruled out of scope for that spec). The ingest summary also never tells the user when a fetch returned the same/no-newer `source_updated` as what's already loaded, so a "successful" refresh can silently be a no-op.
  evidence: Flagged by both reviewers (adversarial + edge-case) of the Admin-page spec (2026-07-12). Root cause (the dated snapshot URL) predates this diff — inherited unchanged from `register/cli.py`/`register/ingest.py` — this just exposes it through a UI button instead of the CLI.
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md` (CAP-1) — `register/db.py:get_current_source_updated()` captures the snapshot's `source_updated` immediately before each refresh; `views/admin.py` compares it to the fetch result and shows `st.info("already up to date …")` instead of the generic success message when they match. Does not fix the underlying frozen-snapshot source (out of scope, unchanged). 12/12 tests passing in `test_ingest.py` (3 new). Live-verified via browser that the confirm flow and error path work; the no-op branch itself was blocked from live verification this session by an unrelated SSL cert gap in the fallback Python interpreter used — recommend the user re-check live under the project's normal `uv run` environment.

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-sponsor-register-ingest.md`
  summary: No concurrency guard around sponsor-register ingest — two overlapping runs (double-click, two browser tabs, or a concurrent reader elsewhere in the app) can race against `replace_all`'s `DELETE`+`INSERT`, surfacing as a raw `sqlite3.OperationalError` ("database is locked") with no `PRAGMA busy_timeout` or button-disable-while-in-flight to soften it.
  evidence: Flagged by both reviewers of the Admin-page spec (2026-07-12). Pre-existing architectural gap — no SQLite connection anywhere in `register/db.py` or `views/*.py` sets `busy_timeout` (only `mcp_server/tools.py` does, specifically for MCP-vs-Streamlit contention) — same theme as the already-logged "no atomic file writes anywhere" item above.
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md` (CAP-2) — `register/db.py:connect()` now sets `PRAGMA busy_timeout = 5000` right after opening every connection (mirrors `mcp_server/tools.py`'s existing pattern/value), so every register-DB caller, not just ingest, waits on a lock instead of failing immediately. Verified via new `test_connect_sets_busy_timeout_pragma`.

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-sponsor-register-ingest.md`
  summary: No confirmation step before the Admin page's "Refresh sponsor register now" button fires a destructive whole-table wipe+reload of the sponsor register — a bigger blast radius than this codebase's other single-click mutations (e.g. "Mark discarded"), which only ever touch one row.
  evidence: Flagged in adversarial review of the Admin-page spec (2026-07-12). Matches this codebase's general no-confirmation convention, but whether to add a confirm step here is a UX design decision, not a trivial patch.
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md` (CAP-3) — "Refresh sponsor register now" now opens an `st.dialog` confirmation ("Cancel" / "Yes, refresh now") before the ingest runs. Live-verified in browser: dialog opens with no mutation, Cancel closes it cleanly with sponsor count unchanged, and the confirm path's own error handling (tested against a real, if environment-broken, network call) shows inline without crashing or partially mutating.

- source_spec: none
  summary: `st.error(str(exc))` — used identically across every view in this codebase, including the new Admin page — can render a blank error box for any exception whose `str()` happens to be empty (e.g. some `OSError`/`HTTPError` subclasses), giving the user no information about what went wrong.
  status: done 2026-07-15
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-error-display-hardening.md` — new `error_display_text(exc: BaseException) -> str` helper in `src/jobs/ui_actions.py` (falls back to `f"{type(exc).__name__}: (no error message)"` when `str(exc).strip()` is empty; also swallows a `__str__` that itself raises). Routed through all 10 sites: `views/admin.py` (4), `views/intake.py` (2), `views/jobs_list.py` (4). Typed `BaseException` rather than `Exception` since 6 of the 10 sites pass a caught `SystemExit`. 6 new tests in `tests/test_ui_actions.py` (non-empty passthrough, empty, whitespace-only, `SystemExit`, and a broken-`__str__` exception). 186/186 tests passing. `grep -rn "st.error(str(exc))" views/` returns no matches.
  evidence: Flagged by the edge-case reviewer of the Admin-page spec (2026-07-12) but applies identically everywhere this pattern is already used (`views/intake.py`, `views/jobs_list.py`), so it's a systemic, pre-existing pattern rather than something specific to any one diff.

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-cv-upload-resume-registration.md`
  summary: `extract_text_from_docx` only reads top-level paragraph text (`doc.paragraphs`) — tables, headers/footers, and text boxes are silently dropped, so a table-based CV layout (skills/dates commonly laid out in tables) can produce an incomplete profile with no error or warning.
  evidence: Flagged by both reviewers of the CV-upload spec (2026-07-12). A deliberate scope choice at spec-authoring time (frozen spec explicitly said "pure python-docx paragraph-join"), not an oversight — but real CVs commonly use tables, so worth revisiting if incomplete profiles turn out to be common in practice.
  status: done 2026-07-16
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-resume-docx-table-extraction.md` — `extract_text_from_docx` now walks `document.iter_inner_content()` (python-docx's public order-preserving API) so table rows land in their original document position alongside paragraphs. Review caught and fixed three real edge cases before merge: an all-blank table row producing a non-empty `" | "` that would have defeated the caller's "no text found" guard, horizontally-merged cells duplicating their text, and multi-paragraph cells injecting stray newlines. Headers, footers, and text boxes remain out of scope (narrower, deliberate cut); `w:sdt` content-control-wrapped sections specifically are logged as a new, separate deferred item below since even the official python-docx API skips them. 221/221 tests passing (4 new).

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-cv-upload-resume-registration.md`
  summary: No idempotency/double-click guard on the Admin page's "Extract & Register Profile" button — since `profiles` is insert-only, a double-click or repeat submission creates duplicate rows and burns a Gemini API call each time.
  evidence: Flagged in adversarial review of the CV-upload spec (2026-07-12). Matches a pre-existing, app-wide pattern — no button anywhere in `views/*.py` disables itself while its own action is in flight — not specific to this diff.
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md` (CAP-4) — `views/admin.py` tracks `st.session_state["cv_registration_in_progress"]` and `st.session_state["last_registered_cv_file_id"]` (keyed on Streamlit's stable `uploaded_file.file_id`); the button disables and short-circuits with an info message once the current file has already been registered. Not live-tested (would insert a real row into the user's actual `profile.db` and burn a real Gemini call) — verified via code review; recommend the user click through it themselves.

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-cv-upload-resume-registration.md`
  summary: No confirmation or warning before a new CV upload supersedes the currently "latest" profile — since match-scoring/tailoring always reads `get_latest_profile` (latest wins), uploading a new CV silently changes behavior for every job already in the pipeline.
  evidence: Flagged in adversarial review of the CV-upload spec (2026-07-12). Same theme as the already-logged "no confirmation before destructive sponsor-register replace" item above — a UX design decision, not a trivial patch.
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md` (CAP-5) — after extraction, if a profile already exists, `views/admin.py` opens an `st.dialog` showing the current vs. candidate profile (name/seniority) and only calls `insert_profile` on explicit confirm; skipped entirely when there's no existing profile to supersede. Reuses the same `st.dialog` mechanism proven live for CAP-3. Not live-tested (would touch the user's real `profile.db` and burn a real Gemini call) — verified via code review; recommend the user click through it themselves.

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md`
  summary: `register/db.py`'s `PRAGMA busy_timeout = 5000` may be undersized for the operation it's meant to protect — `replace_all()` holds an exclusive write lock for the full `DELETE`+`INSERT` of the ~142k-row register inside one transaction, and 5000ms was chosen only because it matches `mcp_server/tools.py`'s existing value/convention, not because the actual reload duration was measured against it.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-14) of the admin-destructive-safety diff. Not a regression from this diff's stated success criteria (CAP-2's success is "no unhandled OperationalError," which still holds even if the timeout occasionally isn't enough — the failure just degrades to a caught, displayed error instead of a crash), but worth measuring/tuning if it turns out to bite in practice.
  status: done 2026-07-16
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-register-busy-timeout-tuning.md` — measured `replace_all()` directly against the real local 142,235-row `data/sponsors.db` (3 trials: 553.7/614.9/635.3ms), then bumped `register/db.py`'s `busy_timeout` to a named `BUSY_TIMEOUT_MS = 15000` constant (~24x the worst trial), leaving `mcp_server/tools.py`'s and `resume/db.py`'s unrelated `busy_timeout=5000` untouched. Review caught a critical bug before merge: `mcp_server/tools.py`'s `check_sponsor()` opened its connection via `connect_register()` (this same `register.db.connect()`) and then immediately re-executed `PRAGMA busy_timeout = 5000` on that connection, silently downgrading it right back down for exactly the concurrent-reader path most likely to race an admin-triggered refresh — removed that redundant line. Also added a real lock-contention test (one connection holds a write lock, a second with the new timeout waits-then-succeeds instead of raising `database is locked`) since the original perf-only test never exercised what `busy_timeout` actually does, and strengthened the perf test with more realistic (nullable-field, varied-length) synthetic data and an actual `count(conn)` persistence check. 223/223 tests passing (2 new).

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md`
  summary: `pyproject.toml` pins `streamlit` with no version floor at all, yet the admin-destructive-safety diff added a hard dependency on `st.dialog` (added in Streamlit 1.31) and `UploadedFile.file_id` — an environment that resolves an older Streamlit would fail outright with no indication in the manifest of why.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-14). Matches this project's pre-existing, deliberate convention of unbounded version floors on every dependency (not just streamlit) per `project-context.md`, so singling out streamlit alone would be inconsistent — worth revisiting as a project-wide pinning policy decision, not a one-line patch to this diff.
  status: done 2026-07-16
  resolution: Implemented via `_bmad-output/specs/spec-streamlit-version-floor/SPEC.md` (CAP-1) — `pyproject.toml:11` now pins `streamlit>=1.31`, matching the codebase's existing hard dependency on `st.dialog`/`UploadedFile.file_id`. Every other dependency's floor is left unbounded, per the spec's constraints. Committed in 76153c3 ("Pin streamlit>=1.31 in pyproject.toml"). Sweep-verified 2026-07-16 by re-reading `pyproject.toml` directly.

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md`
  summary: The CV-upload "supersede current profile" `st.dialog` (opened when a new CV would replace an existing latest profile) has no way to be dismissed via the modal's native X/backdrop-close — only its own in-dialog "Cancel" button works. Dismissing via X still triggers Streamlit to visually close the modal, but the calling script re-invokes the same `st.dialog` function on the next rerun (since it's gated on `st.session_state["pending_cv_profile"]` staying set), so the modal reopens. The equivalent sponsor-register-refresh dialog does *not* have this bug (it's invoked directly from the triggering button click, not from persisted state, so it naturally stays closed after any dismissal).
  evidence: Flagged by both Blind Hunter and Edge Case Hunter reviews (2026-07-14) of the admin-destructive-safety diff; confirmed via code-path analysis (not live-tested, since reaching this dialog requires a real CV upload against the user's actual profile.db). `st.dialog` has no documented callback for X/backdrop dismissal, so a full fix means either accepting this as an inherent limitation of the primitive (document "use Cancel, not X") or a more involved state-machine redesign — not a trivial patch. Severity is moderate, not high: the in-dialog Cancel button is a fully functional, clearly-labeled way out, so no data loss or permanent lockout results, just a UX papercut.
  status: done 2026-07-17
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-cv-supersede-dialog-dismiss-fix.md` — the "no documented callback" premise no longer held: `st.dialog` gained an `on_dismiss` parameter (confirmed via web search: added in Streamlit 1.48.0, 2025-08-05) that runs a callable specifically on X/backdrop/ESC dismissal. Extracted Cancel's cleanup into a shared `_clear_pending_cv_profile()` helper, wired as `on_dismiss=_clear_pending_cv_profile`. Review caught a critical gap: `pyproject.toml`'s `streamlit>=1.31` floor doesn't guarantee `on_dismiss` support, and since it's applied at decorator/import time, any `>=1.31,<1.48` environment would have crashed the *entire* Admin page on load — bumped the floor to `>=1.48`. Review also mutation-tested the initial tests and proved they missed a Cancel/on_dismiss divergence regression; added a real `AppTest`-based test driving the actual Cancel button through the real dialog (verified this catches the exact gap by mutation-testing it myself before and after the fix). 227/227 tests passing (3 new in `tests/test_admin.py`).

- source_spec: `_bmad-output/implementation-artifacts/spec-admin-destructive-safety.md`
  summary: The CV-upload idempotency guard keys off `uploaded_file.file_id` (a Streamlit upload-widget identity, stable only for the file currently held by that widget instance) rather than a hash of the file's actual content. Re-selecting the identical file via the OS file picker (as opposed to just re-clicking the button on the file already held in the widget) produces a new `file_id` and would bypass the guard, allowing a genuine duplicate profile row and a second Gemini charge.
  evidence: Flagged by both Blind Hunter and Edge Case Hunter reviews (2026-07-14). The guard still correctly covers the primary threat model the original deferred-work item described (double-clicking the button for the file currently in the widget) — this gap is a secondary, lower-probability path. A full fix (hash `raw_text` instead) requires eagerly extracting text on every render just to compute the comparison key, which changes error-timing behavior (a corrupt file would now fail immediately on upload rather than after clicking the button) — a real design tradeoff, not a one-line patch.
  status: done 2026-07-17
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-cv-upload-content-hash-idempotency.md` — the "requires eager text extraction" premise didn't hold: hashing the raw uploaded bytes via `UploadedFile.getvalue()` (a pure in-memory byte digest, verified via `dir(UploadedFile)`) sidesteps the eager-extraction tradeoff entirely, since it can't fail the way text extraction can. `st.session_state["last_registered_cv_hash"]` now replaces the `file_id`-keyed check at both the `already_registered` comparison and the mark-as-registered step. Review caught real gaps in the initial tests (a hand-seeded-hash test alone never proved the write side actually stores a matching hash, nor that `AppTest.file_uploader.set_value()` really mints a fresh `file_id` for identical content rather than that being an unverified assumption) — added an end-to-end test driving the real registration path, mutation-tested to confirm it catches a write-path regression. Residual, accepted limitation (documented in code and the new deferred entry below): hashing raw bytes doesn't catch two files with identical visible content but different bytes (e.g. re-saved by a different Word version). 229/229 tests passing (2 new).

- source_spec: `_bmad-output/implementation-artifacts/spec-cv-upload-content-hash-idempotency.md`
  summary: `views/admin.py`'s CV-upload idempotency guard hashes the raw uploaded bytes, so two files with identical visible/extracted text but different bytes (e.g. the same `.docx` re-saved by a different Word/LibreOffice version, changing internal XML metadata/timestamps but not the visible content) hash differently and are not recognized as duplicates — a narrower version of the original bug remains possible.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-17) of the content-hash-idempotency diff. A deliberate, accepted trade-off, not an oversight: hashing raw bytes (not extracted text) is exactly what avoids the eager-extraction/error-timing tradeoff this fix exists to sidestep; catching this narrower case would require hashing extracted text instead, reopening that tradeoff. Low real-world likelihood (a user re-saving and re-uploading the exact same CV from a different Office version) — worth revisiting only if it turns out to bite in practice.

- source_spec: `_bmad-output/implementation-artifacts/spec-outreach-file-only-storage.md`
  summary: The CLI's `outreach`/`follow-up` commands accept a `--out-dir` override for where the drafted message `.txt` file is written, but the Streamlit UI (`views/jobs_list.py`, `views/intake.py`, `jobs/ui_actions.py`) always reads/writes at the hardcoded `DEFAULT_GENERATED_CV_DIR` — a message drafted via the CLI with a custom `--out-dir` will show as "(message file not found)" in the UI's Message-history expander.
  evidence: Flagged by both reviewers of the outreach-file-storage spec (2026-07-12). Same class of gap the tailored-resume/cover-letter feature already has (the UI never exposes an out-dir override either) — not new to this diff's design, just inherited.

- source_spec: `_bmad-output/implementation-artifacts/spec-outreach-file-only-storage.md`
  summary: If the `.txt` file write fails after `insert_outreach_message`'s DB commit (disk full, permission denied), the metadata row persists with a `char_count` but the actual message text is permanently lost with no warning surfaced anywhere.
  evidence: Flagged by the edge-case reviewer of the outreach-file-storage spec (2026-07-12). Same systemic "no atomic write / no rollback" gap as the already-logged "`.txt`/`.docx` write paths in `jobs/cli.py` don't use temp-file-then-rename" item above — not specific to this diff.
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-atomic-file-writes.md` (CAP-3) — the outreach draft write (`jobs/cli.py:794`) now wraps `_atomic_write_text` in `try/except OSError`, raising `SystemExit` naming the message id/channel/char-count and explicitly stating the drafted text is lost even though its metadata was saved. No compensating DB-row delete (matches this codebase's insert-only convention, a deliberate assumption logged in the spec). Verified via new `test_cmd_outreach_write_failure_after_db_commit_raises_distinct_lost_text_error`, which also confirms the DB row still exists post-failure — the exact orphaned-metadata state the error message now warns about.

- source_spec: `_bmad-output/implementation-artifacts/spec-atomic-file-writes.md`
  summary: `_atomic_write_text`'s temp-then-`os.replace` pattern fsyncs the temp file's data but never fsyncs the containing directory after the rename, so a crash immediately after `os.replace` but before the OS flushes directory metadata can still lose the rename on POSIX. The Windows/NTFS semantics this project actually runs on differ further and are untested either way.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-14) of the atomic-file-writes diff. Not fixed because the precedent this helper deliberately mirrors — `_bmad/scripts/memlog.py`'s own `write_atomic()` — doesn't do directory fsync either, and doing it portably (POSIX directory-fd fsync has no direct Windows equivalent) is real added complexity beyond matching that existing, already-audited pattern.

- source_spec: `_bmad-output/implementation-artifacts/spec-atomic-file-writes.md`
  summary: `_write_tailoring_files` writes the resume and cover-letter files as two independent atomic writes, not one atomic pair — if the resume write succeeds and the cover-letter write then fails, the function leaves a mismatched pair (one file present, one absent) with no distinguishing message, and nothing checks whether downstream cache/already-generated logic expects both files present or just one.
  evidence: Flagged in the Edge Case Hunter review (2026-07-14). Real, but extending CAP-1's contract from "each individual file write is atomic" to "the resume+cover-letter pair is atomic as a set" is a bigger design change (would need two-phase rename or a manifest) than this spec's scope committed to.

- source_spec: `_bmad-output/implementation-artifacts/spec-atomic-file-writes.md`
  summary: After a CAP-3 write failure, retrying re-runs `_draft_and_store_outreach` end to end, which calls `insert_outreach_message` again and commits a *second* DB row - the first row stays permanently orphaned (metadata pointing at a file that never existed). Nothing added by this diff flags or distinguishes that orphaned row later: `_read_outreach_message_text` just returns `None` for a missing file, identical to what it'd return if the file had been deleted for any other reason.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-14). CAP-3 only promised a clear error *at the time of failure* (delivered - see the entry above), not permanent tracking of which historical rows are known-orphaned; that would need a schema/status change, a real design decision beyond this spec's scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-atomic-file-writes.md`
  summary: `src/jobs/docx_tailor.py`'s `document.save(str(out_path))` (used by the `.docx` tailoring path, arguably the higher-traffic path since it serves UI downloads) has no temp-file/atomic-replace protection at all - the exact "half-written file on crash" failure mode this spec's CAP-1/CAP-2 exist to prevent, except a `.docx` is a zip container and arguably more fragile under a partial write than plain text. Separately, `_tailor_docx_for_job` calls `update_tailoring(conn, ...)` (a DB commit) *after* both docx writes succeed - the mirror image of CAP-3's problem, unaddressed.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-14). Explicitly out of this spec's scope (Non-goals: "not resume/db.py, register/db.py, or any other module" - `docx_tailor.py` is the same category of exclusion, just not named explicitly since it wasn't in the original deferred-work entries this spec closed).
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-docx-and-ui-outreach-atomic-writes.md` (CAP-1) — new local `_atomic_write_bytes()` helper in `docx_tailor.py` (mirrors `jobs.cli._atomic_write_text`'s design; kept local, not imported, to avoid a circular import since `jobs/cli.py` already imports `docx_tailor`). `build_tailored_docx`/`write_plain_docx` now save to an in-memory `io.BytesIO()` buffer first, then atomic-write those bytes. Atomicity only, no new exception handling — matches this spec's own precedent, since these writes run *before* `update_tailoring`'s DB commit, not after; the `update_tailoring`-ordering desync mentioned in this entry's summary remains a separate, still-open, lower-severity item. 180/180 tests passing (3 new, including a simulated `os.replace` failure on a real `.docx` build confirming no partial/corrupt file and no orphaned `.tmp`).

- source_spec: `_bmad-output/implementation-artifacts/spec-atomic-file-writes.md`
  summary: `views/ui_actions.py`'s `draft_and_save_outreach` (called from `views/intake.py` and `views/jobs_list.py` - the Streamlit UI's own outreach-drafting entry point, likely the primary one a user actually clicks) does a plain `path.write_text(draft.message, encoding="utf-8")` with no atomic-write helper and no `try/except` at all - the identical committed-DB-row/lost-text failure mode this spec's CAP-3 just hardened in the CLI path (`jobs/cli.py`'s `_draft_and_store_outreach`), left completely untouched.
  evidence: Flagged in the Edge Case Hunter review (2026-07-14), found incidentally while tracing the same write pattern outward from the diff. Real and arguably higher-priority than the CLI path this spec covers, but explicitly out of scope (this spec's Non-goals limited it to the four named `jobs/cli.py` sites) - worth a follow-up spec given it's the UI's primary entry point.
  status: done 2026-07-14
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-docx-and-ui-outreach-atomic-writes.md` (CAP-2) — `src/jobs/ui_actions.py`'s `draft_and_save_outreach` (note: the file lives at `src/jobs/ui_actions.py`, not `views/ui_actions.py` as this entry's summary said; it's imported into `views/intake.py`/`views/jobs_list.py`) now reuses `jobs.cli._atomic_write_text` and wraps the write in `try/except (OSError, ValueError)`, raising `SystemExit` with the drafted text embedded directly in the message — not `print()`'d like the CLI fix, since a Streamlit user never sees server stdout, only `st.error(str(exc))`. New `tests/test_ui_actions.py` (this module had zero prior coverage) verifies both the happy path and that a failure's exception message contains the recoverable text. 180/180 tests passing.

- source_spec: `_bmad-output/implementation-artifacts/spec-error-display-hardening.md`
  summary: No test asserts that `views/admin.py`/`views/intake.py`/`views/jobs_list.py` actually call the new `error_display_text` helper at their 10 error-display sites — all test coverage is at the helper's unit level, so a future typo or partial revert at any call site would regress silently past the full suite.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-15) of the error-display-hardening diff. Not trivially fixable: `views/*.py` are Streamlit page scripts that execute top-level code as a side effect of import (same reason `app.py`'s secrets-bridging logic has no test — see the separate deferred entry on that), so asserting call-site wiring would need either a Streamlit AppTest harness or extracting the error-handling logic out of the view scripts — a real testing-infrastructure decision, not a one-line patch.
  status: done 2026-07-15
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-streamlit-view-testability.md` (CAP-2) — new `tests/test_views_error_display.py` uses `AppTest` to run each of the 3 view files as a real script and force an empty-`str()` exception at one representative call site per file (`admin.py`'s module-level DB-connect, `intake.py`/`jobs_list.py`'s tailor button), asserting the rendered error names the exception type. Covers 3 of the 10 call sites by design (catches a systemic revert, not exhaustive per-site coverage — see this spec's own Non-goals). The `jobs_list.py` test seeds 2 jobs and asserts the mock was called with the *second* job's id specifically, catching a wrong-loop-variable/closure bug a single-job test couldn't. 191/191 tests passing.

- source_spec: `_bmad-output/implementation-artifacts/spec-error-display-hardening.md`
  summary: `error_display_text`'s fallback string (`f"{type(exc).__name__}: (no error message)"`) reads like an internal repr rather than user-facing copy, and doesn't surface `__cause__`/`__context__` when the empty-message exception was chained from a more informative one.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-15). Explicitly out of this spec's scope (Non-goals: "not... a redesign of error presentation" — the spec's CAP-2 only requires the fallback be non-empty and name the exception type, which it does). Worth revisiting if users report the fallback itself being unhelpful in practice.

- source_spec: `_bmad-output/implementation-artifacts/spec-streamlit-view-testability.md`
  summary: `tests/conftest.py`'s `streamlit_data_env` fixture returns only `tmp_path`, forcing every consumer to re-derive `tmp_path / "data" / "jobs.db"`-style paths inline (as `tests/test_views_error_display.py`'s `_seed_one_job` does) instead of the fixture handing back ready-made paths.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-15) of the streamlit-view-testability diff. Minor DRY/ergonomics gap, not a correctness issue — but the fixture's return shape (plain path vs. a dict/dataclass of pre-built DB paths) is a design choice worth deciding once, before more test files start depending on it and lock in the current shape by precedent.
  status: done 2026-07-16
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-streamlit-data-env-fixture-paths.md` — confirmed the prediction had already happened (4 real call sites duplicating the path-join across 2 files) before fixing it. `streamlit_data_env` now returns `{"root", "jobs_db", "profile_db", "sponsors_db"}`, matching this test suite's own existing `ui_tailor_env` dict-return convention rather than inventing a new shape. Review caught a real DRY gap in the fix itself (the seeding calls and the returned dict built the same path two different ways) and a coverage gap (no test asserted the fixture's own return-shape contract, so `root`/`profile_db`/`sponsors_db` had zero verification); both fixed, plus a new `tests/test_conftest.py`. 224/224 tests passing (1 new).

- source_spec: `_bmad-output/implementation-artifacts/spec-force-regenerate-control.md`
  summary: Neither `generate_tailored_application` (`jobs/tailor.py`) nor `generate_paragraph_edits` (`jobs/docx_tailor.py`) wraps its `genai.Client()` call in a try/except, so a network error, API error, or malformed-JSON `model_validate_json` failure propagates as a raw, unhandled exception rather than the friendly `SystemExit`-based error message this codebase uses everywhere else. Both `views/intake.py` and `views/jobs_list.py` only catch `except SystemExit`, so this surfaces as a raw Streamlit traceback.
  evidence: Flagged in the Edge Case Hunter review (2026-07-15) of the force-regenerate-control diff. Pre-existing gap, already documented in `project-context.md`'s "Known gap (not yet fixed, just documented)" note about `extract.py`/`tailor.py` having no handling for a Gemini response that fails pydantic validation — not introduced by this diff. This diff does widen where it's reachable: previously "Regenerate" was a guaranteed cache hit (never actually called the LLM again), so only first-time "Generate" clicks could hit this gap; now both paths can.
  status: done 2026-07-16
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-tailoring-llm-error-handling.md` — both functions now catch `(genai_errors.APIError, genai_errors.UnknownApiResponseError, httpx.HTTPError, ValidationError, RuntimeError)` and re-raise `SystemExit(f"...: {detail}")` (`detail` falls back to the exception's type name when `str(exc)` is empty), matching this codebase's existing convention. The exact exception set was resolved by reading the installed `google-genai` SDK source rather than guessing, and review (Blind Hunter + Edge Case Hunter) found the initial tuple missed two real, SDK-confirmed failure modes — bare `RuntimeError` on missing/invalid API credentials, and `UnknownApiResponseError` (a `ValueError` subclass, not `APIError`) on a non-JSON 200 response — both patched in before merge. 213/213 tests passing (18 new: 9 per function covering API error, network error, missing credentials, unknown-response, malformed JSON, non-JSON text, empty-message fallback, exception chaining, and unrelated-exception passthrough). `jobs/extract.py` still has the identical gap (`project-context.md`'s Known-gap note) — out of this spec's scope, still open.

- source_spec: `_bmad-output/implementation-artifacts/spec-force-regenerate-control.md`
  summary: No concurrency guard around docx regeneration — two browser tabs/sessions both seeing a valid cache and both clicking "Regenerate" will both bypass the cache, both make a real LLM call, and both overwrite the same docx files and `jobs.db` tailoring columns; last write wins, silently discarding the other session's LLM output and API spend.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-15). Matches this codebase's existing "no concurrency guard" theme (e.g. the already-logged sponsor-register-ingest race, mitigated there only by a `busy_timeout` PRAGMA + confirmation dialog, not a real lock). Pre-existing category of risk, but newly more reachable here since this diff turns "Regenerate" from a harmless no-op into a real, repeatable mutation.

- source_spec: `_bmad-output/implementation-artifacts/spec-tailoring-llm-error-handling.md`
  summary: When a Gemini call fails in `generate_tailored_application`/`generate_paragraph_edits`, the original exception is converted straight to `SystemExit` with no server-side logging — once `st.error(...)` shows the message to the user, the full exception/traceback is gone, leaving near-zero diagnosability for a real production failure.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-16) of the tailoring-llm-error-handling diff. Matches this codebase's systemic, pre-existing convention — every other `SystemExit`-based failure site (e.g. `_require_raw_resume_text`) has the identical property, so this isn't new to this diff, just newly reachable via two more failure modes. A real fix (e.g. `logging` before the `raise`) is a project-wide observability decision, not a one-line patch to this spec.
  status: done 2026-07-16
  resolution: Implemented via `_bmad-output/implementation-artifacts/spec-tailoring-llm-failure-logging.md` — both functions now call `traceback.print_exc(file=sys.stderr)` inside their except block, before `raise SystemExit`, wrapped in its own `try/except Exception: pass` so the diagnostic itself can never break the `SystemExit` contract. Matches `app.py`'s existing stderr-diagnostic precedent rather than introducing the `logging` module (this codebase has zero prior `logging` usage). Review caught that the initial tests only exercised one exception type with weak assertions; strengthened to check message content, call-site attribution, and clean stdout, plus added coverage for the structurally distinct `ValidationError`-from-parsing path. 217/217 tests passing (4 new).

- source_spec: `_bmad-output/implementation-artifacts/spec-resume-docx-table-extraction.md`
  summary: `extract_text_from_docx` still silently drops any paragraph or table wrapped in a `w:sdt` (structured document tag / content control) element — Word's built-in resume/CV templates commonly wrap sections (job title, dates, etc.) in content controls, and even python-docx's own public `iter_inner_content()` API (used by this fix for the ordinary `w:p`/`w:tbl` case) skips `w:sdt` entirely.
  evidence: Flagged in the Blind Hunter adversarial review (2026-07-16) of the resume-docx-table-extraction diff. Same class of "silent content loss" this spec exists to fix, just triggered by a different Word feature than tables — not addressed because content controls can nest arbitrarily, so a correct fix needs its own design/research pass, not a one-shot patch. Matches the nested-tables exclusion this same spec already named as a non-goal.
