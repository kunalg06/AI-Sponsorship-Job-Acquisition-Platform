---
topic: AI Sponsorship Job Acquisition Platform - new project brainstorm
goal: stress-test and shape a new multi-agent job-search automation platform PRD before build
status: complete
updated: 2026-07-20T23:59
---

- (decision) User is building this for personal use only - one candidate applying to UK sponsorship jobs with their own resume, not a multi-tenant SaaS platform
- (dynamic) Confirmed real UK sponsor register CSV (gov.uk, updates periodically): 142,236 total rows, ~122,355 on Skilled Worker route. Columns are only Organisation Name, Town/City, County, Type&Rating, Route - zero industry/sector signal, so Agent 0/1 classification must come from external enrichment, not the register itself
- (decision) MVP shape narrowed further: user sources jobs manually across scattered platforms/ATSs (job discovery not automated), pastes/shares the posting into the system, which does sponsor-license lookup + match score + tailored resume/cover letter + cold DM draft in under 60s; human sends/applies. Removes most scraping/Playwright/ToS risk from V1. User's stated belief: same ATS vendor reuses/repeats the first scan result across different companies on that vendor, making cold DM to a human the higher-leverage channel - stated as belief, not yet verified
- (decision) Added feature: application tracker (mark applied/discarded per job, supports same company + multiple roles as separate entries) with a 3-day-after-applying follow-up reminder. User has a third-party Chrome extension 'UK Visa Sponsorship Check' but distrusts its accuracy and prefers relying on the official gov.uk sponsor register CSV as source of truth
- (decision) Reminder cadence locked to repeating day 3/7/14 nudges (not auto-sent, just surfaced with a drafted follow-up). User will feed profile via CV + GitHub + LinkedIn, and asked for more features to make applying easier and get more attention on sponsorship roles
- (outcome) CONVERGED V1 scope locked. Core loop: cleaned sponsor register (gov.uk CSV) + job-post paste-in intake + sponsor-status check (3-case: direct/agency-redacted/user-supplied) + SOC-code/going-rate/salary-list threshold check + match scoring + tailored resume/cover letter (GitHub-evidence-backed) + channel-aware cold DM drafts (narrative-core-driven) + application tracker (company+role key) with repeating day 3/7/14 reminders + Goal/Roadmap Planner (Dec-2026 UK sponsorship deadline, DSA/AI-system-design/interview prep, real-deadline-aware goal readjustment, no sugarcoating) - all placed in core loop per user due to deadline pressure. Fast-follow: daily coach digest, Gmail integration, LinkedIn/one-pager personal brand assets, outcome feedback loop, referral/mutual-connection nudges, portfolio-gap-driven interview prep. No LangGraph/Qdrant/Playwright/scraping needed for V1 - simple pipeline + small DB, greenfield project confirmed (no code/stack/resume file exist yet)
- (outcome) Session wrapped: wrote V1 scope doc to docs/v1-scope.md and a dossier-styled keepsake HTML to _bmad-output/party-mode/2026-07-05-session-keepsake.html. Room dynamic: Mary (evidence/data), John (job-to-be-done pressure), Winston (right-sizing infra), Victor (personal-tool reframe), Murat (ToS/risk), Sally (UX friction), Dr. Quinn (root cause), Carson (energy/synthesis), Maya (coach empathy) were the most active voices this session
- (decision) User rejected web-search-for-verification in favor of a sharper fix: surface location (town/city) on sponsor fuzzy-match candidates so the user can compare against the job posting's stated location themselves, plus a manual override command for when they've verified sponsorship via their own research (e.g. browser extension) - no new external dependency needed, reuses register data already fetched
- (outcome) Built and verified: sponsor fuzzy-match now surfaces every candidate's town/city/county so the user can compare against the job posting's stated location before trusting a match; added 'jobs confirm-sponsor' for manually asserting a verdict (status=user_confirmed) once verified via own research (location match, browser extension). Real test: Bending Spoons posting says Cambridge, register's UK branch is London - user now sees that gap directly instead of a false confident match. 94 tests passing
- (outcome) Built and verified format-preserving DOCX tailoring: jobs/docx_tailor.py extracts paragraphs from the user's real source resume (cv/my-resume/Kunal_G_CV.docx, confirmed exactly 2 pages/949 words/5410 chars via Word's own cached app.xml metadata), LLM classifies each paragraph as keep-vs-rewrite (style names alone weren't reliable since the real doc reuses styles inconsistently), python-docx swaps only text in existing runs so fonts/bold/size never change. Live-verified on the real Bending Spoons job: 62/62 paragraphs preserved, all structural lines (name/headers/dates/education/URLs) untouched, all content bullets reworded toward data-science framing while keeping every real metric (92%, 25%, 80%, 68%, 50%) intact, no page-overflow warning triggered. New CLI: jobs tailor-docx <id>, output to cv/generated_cv/<company>/resume.docx + cover_letter.docx. Next: user wants a Streamlit UI (confirmed choice) so they can use this without a Claude session, with download buttons for generated_cv files
- (outcome) UI redesign review (session mode): Sally/Maya/Caravaggio/John/Winston + Amelia walk-on critiqued app.py + the two new Streamlit pages. Top findings: (1) real bug - tailoring/outreach were gated behind session_state.saved_job_id, so revisiting an old job required re-pasting its text, which silently duplicated the row (insert_job has no dedup); (2) main page conflates a linear wizard with two global dashboard sections (Application Tracker, generated-CV browser) that belong on Jobs List instead; (3) sidebar nav label 'app' has no icon/title despite streamlit 1.58 supporting st.navigation() for real titles; (4) no visual theme/identity, judged lower priority than functional fixes for a single-user tool; (5) candidate-picker radio list gets heavy at MAX_FUZZY_CANDIDATES=10, minor. User picked (1) to fix first.
- (outcome) Fixed: added src/jobs/ui_actions.py (generate_tailored_docx_for_job, draft_and_save_outreach) taking a plain job_id instead of session_state. app.py refactored to use it (removed ~90 lines of duplicated logic across two call sites). pages/2_Jobs_List.py gained matching 'Tailored Resume & Cover Letter' and 'Recruiter Outreach' sections so any stored job - not just the one just pasted - can be tailored/messaged. Live-verified: generated a fresh tailor + outreach draft for a test job entirely from the Jobs List page, with zero dependency on saved_job_id (grepped for it - zero references). 111 tests still pass. Test data cleaned up; only the real Bending Spoons job/docs remain.
- (outcome) Fixed the other two UI findings from the earlier design review. (1) Nav labels: migrated app.py to st.navigation()/st.Page() (verified via docs that pages/ is fully ignored once st.navigation runs). New structure: app.py is now a thin router, real page content moved to views/intake.py, views/roadmap.py, views/jobs_list.py; old pages/ dir deleted. Sidebar now shows real icons+titles (Paste a Job Posting / Roadmap / Jobs List) instead of the bare 'app'. (2) Wizard/dashboard split: moved Application Tracker and the generated-CV browser off the intake page onto Jobs List (collapsed expanders at the top, above the per-job browser) - matching Maya's framing that those are global dashboard views, not part of the linear paste-job wizard. Added a one-line wayfinding caption at the bottom of intake pointing to Jobs List. Verified live: nav renders correctly, all three pages work with real data (Roadmap 157 days remaining, Jobs List showing the real Bending Spoons job + its tailored docs via the moved browser section), 111 tests still pass. Remaining open item from the review: the visual theme pass, still deferred as lower priority for a single-user tool.
- (outcome) Visual theme pass done - the last item from the design review, previously deferred as lowest priority. Config-only change (.streamlit/config.toml, no custom CSS): warm brass/amber accent (#D99A3D dark, #B87B1E light) replacing Streamlit's stock red, against a warm charcoal-navy dark background or warm paper-white light background, small border radius, sidebar border. Palette ties into the compass/wayfinding icon identity already established (nav pages use compass/map/clipboard icons). Deliberately avoided the generic-AI-tool looks (cream+terracotta, neon-on-black, purple gradient) per general design taste, and deliberately skipped custom @font-face/CSS to keep it a single safe, reversible config file - matches the 'cheap and load-bearing over gorgeous and imaginary' agreement Winston and Caravaggio landed on earlier. Verified live in both light and dark mode (toggle in Streamlit's own settings menu) across all three pages - buttons, focus rings, progress bars, radios all re-themed correctly, no console errors, 111 tests unaffected. This closes out every item from the original UI redesign review.
- (outcome) Recurring-gap-theme debate (spec-daily-coach-digest follow-up): resolved to exclude discarded jobs from list_recurring_portfolio_gaps counting. Victor's trust argument won on principle; Amelia's live data pull (1 job with gaps, 0 themes either way) showed the debate was currently moot in practice but worth deciding cheap now rather than letting an unreviewed default calcify. Carson's dual-count ('3 live, 1 discarded') display idea shelved, not dead - revisit only if real data ever earns the nuance.
- (dynamic by sally) Sally updated her own position mid-argument once Amelia's real numbers came back - 'don't discard the signal, we don't know why' gave way to 'ship the simple version, a real complaint later beats a hypothetical one now.' Data-driven, not just a UX-friction voice.
- (callback) Winston + Caravaggio's 'cheap and load-bearing over gorgeous and imaginary' line got invoked again (2nd session running) to justify the plain-exclusion fix over Carson's fancier dual-count display.
- (outcome) Built the remote-from-anywhere sponsor-check bypass: SKIPPED status (sponsor_check.py), narrowly worded checkbox on intake ('remote from anywhere - no UK work authorization required', deliberately not just 'remote', per Sally's UK-remote-still-needs-sponsorship point), CLI skip-sponsor-check/--undo for retrofitting old jobs, blue badge in both views. No new DB column - reused existing sponsor_status/sponsor_reason (Winston's 'cheap and load-bearing' line invoked again). 347 tests passing incl. new AppTest coverage of the checkbox path.
- (outcome) Live bug hit right after the sponsor-bypass work: tailoring generation crashed with a pydantic JSON validation error - Gemini's response ran away into a repetition loop (750k+ chars in one field) and got cut off mid-object. Fixed in jobs/tailor.py: generation_config (max_output_tokens=8192, presence_penalty=0.4) bounds the response, plus one retry on ValidationError since a degenerate generation is usually a one-off. 350 tests passing.
- (outcome) Pushed both changes to origin/master for Streamlit Cloud auto-deploy (093b5ca sponsor-bypass feature, f080385 tailoring JSON-truncation fix). Also surfaced a separate, unfixed bug found along the way: local data/jobs.db predates the Turso embedded-replica migration and libsql refuses to open it ('metadata file does not exist') - left untouched since it's real user data, not part of this session's asks.
- (outcome) Fixed the local jobs.db Turso sync bug for real (all 4 local dbs had it: jobs/profile/sponsors/roadmap - any pre-Turso plain-sqlite file makes libsql refuse to open it, 'metadata file does not exist'). dbcompat.connect() now catches that specific ValueError, quarantines the legacy file to <path>.pre-turso-backup (never deletes), and retries as a fresh replica synced from Turso. Verified live against the user's real Turso remote: local jobs.db healed, now shows all 4 real jobs incl. one only Turso had. Also closed a related gap: .gitignore only covered data/*.db, not the new libsql sidecars (-info/-shm/-wal) or the quarantine backup - same personal data, was about to go untracked-visible.
- (outcome) Ran the app for real locally (Playwright/claude-in-chrome against a fresh You can now view your Streamlit app in your browser. Local URL: http://localhost:8501 Network URL: http://192.168.0.7:8501) and it surfaced two more real bugs beyond the DB sync fix: (1) httpx/certifi TLS verification fails on this machine for ANY https host - fixed with truststore.inject_into_ssl() at both entrypoints (app.py, jobs/cli.py), same root cause as [tool.uv] system-certs=true; (2) every genai.Client().interactions.create() call site catches google.genai.errors.APIError, but interactions.create() actually raises a same-named unrelated hierarchy under google.genai._gaos - a live APIConnectionError sailed through uncaught and crashed the page. Fixed in tailor.py+docx_tailor.py (the only 2 of 6 call sites with any error handling at all); extract.py/match_score.py/outreach.py/resume/extract.py have ZERO error handling and were flagged to the user, not yet fixed - open item. Also found generation_config's presence_penalty is SDK-stub-valid but API-rejects-it live (400) - dropped it, max_output_tokens alone confirmed sufficient. End-to-end: generated a real tailored resume+cover letter against live Gemini through the actual UI. Pushed as 2 commits (8d7eb87, 500d1dd).
