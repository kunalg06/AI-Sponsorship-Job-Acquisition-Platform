---
topic: Extend atomic-write hardening to docx_tailor.py's document.save() calls and views/ui_actions.py's outreach write
goal: Close 2 deferred-work.md entries flagged by both reviewers of spec-atomic-file-writes.md as the same root cause left unaddressed in higher-traffic paths
updated: 2026-07-14T23:24
---

- (direction) Source: deferred-work.md, 2 entries flagged in the 2026-07-14 Blind Hunter and Edge Case Hunter reviews of spec-atomic-file-writes.md, both citing the same root cause that spec already fixed in jobs/cli.py, left unaddressed in two other paths.
- (capability) CAP-1: build_tailored_docx and write_plain_docx (src/jobs/docx_tailor.py) write their resulting .docx bytes atomically, so a crash or failure mid-write never leaves a partial/corrupt file at the target path. Source: docx_tailor.py:159 and :203, both currently plain document.save(str(out_path)).
- (capability) CAP-2: views/ui_actions.py's draft_and_save_outreach write becomes atomic and, on failure after insert_outreach_message's DB commit, raises a SystemExit naming the text-lost/metadata-saved mismatch -- mirroring jobs/cli.py's CLI-side outreach path, hardened in the prior spec-atomic-file-writes.md. Source: ui_actions.py:99 (path.write_text), DB commit at :89-91.
- (constraint) CAP-1's atomic-write helper must be implemented locally inside docx_tailor.py (same temp-file-then-os.replace mechanism, pid+uuid-suffixed same-directory tmp naming, cleanup-on-failure -- mirroring jobs.cli._atomic_write_text's design exactly) rather than imported from jobs.cli, because jobs/cli.py already imports docx_tailor -- importing back would create a circular import.
- (constraint) CAP-1 must save via an in-memory io.BytesIO() buffer first (python-docx's Document.save() accepts a file-like stream), then atomic-write those bytes to the final path -- document.save() has no atomic-replace option when writing directly to a path string.
- (constraint) CAP-2 must reuse jobs.cli._atomic_write_text directly, not reimplement it -- matches this codebase's established 'views import CLI-layer helpers directly' convention (ui_actions.py already imports several _-prefixed helpers from jobs.cli) and keeps the single-shared-mechanism principle from the prior spec.
- (constraint) No new dependencies -- io.BytesIO, the same temp-naming/replace pattern, and os.replace are all stdlib, matching the prior spec's helper.
- (constraint) Preserve existing directory-creation behavior at each site (out_path.parent.mkdir(parents=True, exist_ok=True) / path.parent.mkdir(...)) -- this fix targets write atomicity only.
- (assumption) CAP-2's recovery text goes inside the SystemExit's exception message itself, not printed to stdout (unlike the CLI-side fix) -- print() output is invisible to a Streamlit user (it only reaches the server console), while the exception message is what actually reaches the user via the existing 'except SystemExit as exc: st.error(str(exc))' pattern in views/intake.py and views/jobs_list.py. Keeps this spec from having to touch those view files.
- (assumption) CAP-1 gets atomicity only, no new exception-handling -- matches the prior spec's precedent for the tailoring-text write sites (CAP-1/CAP-2 there): _tailor_docx_for_job's update_tailoring DB commit runs AFTER both docx writes succeed, so a write failure here happens before any commit and isn't a DB/file desync case the way the outreach write is. That desync risk (write succeeds, later commit fails) is a separate, already-logged, lower-severity deferred-work item, explicitly out of scope.
- (non-goal) Not touching _tailor_docx_for_job's update_tailoring-after-write ordering/desync risk -- separate, already-logged deferred-work item.
- (non-goal) Not adding a way to distinguish a permanently-orphaned outreach row from a file missing for other reasons -- already-logged deferred-work item, needs a schema change.
- (non-goal) Not touching jobs/cli.py's outreach path further -- already hardened in the prior spec; CAP-2 only extends the same fix to the UI's copy.
- (non-goal) Not adding directory-fsync durability -- matches the established precedent and the prior spec's same scope decision.
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS. Both capabilities have intent+success; intents stay WHAT-level; all 5 constraints rule something out; 4 non-goals present; success signal demonstrable per-capability; CAP-1/CAP-2 fresh and unique; prose is lean.
- (event) Self-validate pass 2 (preservation): PASS. Both deferred-work.md source entries' claims landed: docx unguarded-write + higher-traffic + zip-fragility framing in CAP-1/Why; UI outreach identical-gap framing in CAP-2/Why; the write-before-commit ordering that justifies CAP-1 getting no new exception handling preserved in Assumptions/Non-goals. No companions needed, nothing dropped as wrapper-only.
- (event) Spec finalized: SPEC.md written, 2 capabilities, 0 companions, 0 open questions, 2 assumptions (recovery-text placement, CAP-1 scope vs CAP-2).
