---
topic: Pin a version floor on the streamlit dependency in pyproject.toml to document its already-true st.dialog/file_id API requirement
updated: 2026-07-15T23:50
---

- (decision) Scope this narrowly to streamlit's own floor, not a project-wide pinning policy change. Every other dependency (pydantic, google-genai, python-docx, mcp, pytest) keeps its existing unbounded floor unchanged - this corrects one specific, already-true API requirement (st.dialog, UploadedFile.file_id both need Streamlit >=1.31), not a general 'should we pin everything' policy question the original deferred-work entry worried about.
- (capability) CAP-1: pyproject.toml's streamlit dependency declares a >=1.31 floor, documenting the real API requirement views/admin.py already has (st.dialog, UploadedFile.file_id). Success: pyproject.toml's streamlit entry reads '>=1.31' or higher; a fresh dependency resolve against an environment with streamlit<1.31 fails at install time with a clear version-conflict message instead of failing later at runtime with an unrelated AttributeError.
- (constraint) No other dependency's version specifier changes in this diff - matches this project's existing convention of unbounded floors elsewhere, deliberately not revisited here.
- (direction) Non-goal: not a general dependency-pinning policy decision for the whole project (the separate, still-open deferred-work item about requirements.txt's floating -e . install stays untouched).
- (direction) Non-goal: not adding CI or any automated check that verifies the floor is respected - this project intentionally has no CI.
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS. 1 capability with intent+success; constraint bends design (rules out touching other deps); 2 non-goals; success signal concrete/demonstrable; capability ID stable (new spec); prose lean.
- (event) Self-validate pass 2 (preservation): PASS. All source claims landed -- deferred-work entry text and root cause (Why), specific APIs st.dialog/file_id (Why), narrow-scope decision vs other deps (Constraints), requirements.txt separateness (Non-goals). No companion needed.
- (event) Spec finalized: created, 1 capability (CAP-1), 0 companions, 0 open questions, 0 assumptions.
