---
topic: Admin page destructive-action safety guards
goal: Close 5 adversarial-review-flagged gaps around the Admin page's two destructive/mutating actions (sponsor-register refresh, CV upload/profile registration)
updated: 2026-07-14T21:12
---

- (direction) Source: deferred-work.md, 5 entries flagged in 2026-07-12 adversarial+edge-case reviews of spec-admin-sponsor-register-ingest.md and spec-admin-cv-upload-resume-registration.md, reconfirmed live against code on 2026-07-14 sweep triage. All 5 still open.
- (capability) CAP-1: sponsor-register refresh detects and surfaces a no-op — when ingest's returned source_updated matches what's already loaded, the Admin page shows a distinct message instead of a generic success. Source: views/admin.py:52-56, register/ingest.py.
- (capability) CAP-2: sponsor-register replace_all gets a concurrency guard (PRAGMA busy_timeout) so two overlapping refreshes wait/complete instead of one raising a raw sqlite3.OperationalError. Source: register/db.py has no busy_timeout today; mcp_server/tools.py:103,140,181 already sets PRAGMA busy_timeout = 5000 on every connection open as the established project pattern.
- (capability) CAP-3: 'Refresh sponsor register now' requires an explicit confirm step before the wipe+reload fires (bigger blast radius than the app's other single-click mutations, e.g. Mark discarded, which only touch one row). Source: views/admin.py:45.
- (capability) CAP-4: 'Extract & Register Profile' gets an idempotency/in-flight guard so a double-click or repeat submission can't create duplicate profile rows or burn an extra Gemini API call. Source: views/admin.py:75; profiles table is insert-only (project-context.md).
- (capability) CAP-5: uploading a new CV requires explicit confirmation before it supersedes the currently-latest profile, since match-scoring/tailoring always reads get_latest_profile (latest wins) and every job already in the pipeline is silently affected otherwise. Source: views/admin.py:75-100.
- (constraint) Must reuse the exact PRAGMA busy_timeout = 5000 value/style already used in mcp_server/tools.py — don't invent a different timeout or mechanism for CAP-2.
- (constraint) No new dependencies. Streamlit 1.58.0 is pinned (uv.lock) — st.dialog (native modal, available since Streamlit 1.31) is usable for CAP-3/CAP-5 confirmation UX instead of inventing a custom two-click pattern.
- (constraint) No st.cache_data/st.cache_resource anywhere in this codebase by explicit project convention (project-context.md) — CAP-1's no-op detection and CAP-4's in-flight guard must work via st.session_state across Streamlit reruns, not caching.
- (assumption) st.dialog is available and is the intended confirmation mechanism for CAP-3 and CAP-5, given the pinned Streamlit version (1.58.0) postdates its introduction (1.31) and no lighter-weight in-repo confirm pattern exists yet to match instead.
- (non-goal) Not adding authentication/access control to the Admin page — single-user local tool, out of scope.
- (non-goal) Not building a general-purpose confirmation-dialog component — only these two specific destructive actions (CAP-3, CAP-5) are guarded.
- (non-goal) Not fixing the systemic st.error(str(exc)) blank-box issue or the broader no-atomic-file-writes hardening item — both are separate, already-logged deferred-work entries tracked independently.
- (non-goal) Not changing register.cli.DEFAULT_SOURCE's frozen-snapshot behavior (pointing it at a live 'latest register' endpoint) — CAP-1 only surfaces when a refresh was a no-op, it does not fix the underlying data staleness.
- (non-goal) Not adding a way to delete or roll back a superseded CV profile — CAP-5's confirmation only gates the upload going forward; the profiles table stays insert-only per existing project convention.
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS. All 5 capabilities have intent+success; intents are WHAT-level (CAP-2's 'concurrency guard' names the capability class, mechanism specifics live in Constraints); all 3 constraints rule something out; 5 non-goals present; success signal is demonstrable per-capability and as a whole; CAP-1..5 fresh and unique; prose is lean.
- (event) Self-validate pass 2 (preservation): PASS. All 5 deferred-work.md gaps (no-op detection, busy_timeout, refresh confirm, CV-upload idempotency, CV-supersede confirm) landed as CAP-1..5; sourcing/dates preserved in Why; busy_timeout value/pattern and Streamlit-dialog assumption preserved in Constraints/Assumptions; no companions needed, nothing dropped as wrapper-only.
- (event) Spec finalized: SPEC.md written, 5 capabilities, 0 companions, 0 open questions, 1 assumption (st.dialog availability).
